/**
 * Built-in ruleset version — bump when rule semantics or defaults change materially.
 * Override via `WafOptions.rulesetVersion` for audit correlation in forked builds.
 */
export declare const RULESET_VERSION = "2026.03";
